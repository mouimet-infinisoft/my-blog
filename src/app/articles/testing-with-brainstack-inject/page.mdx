import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
  author: 'Martin Ouimet',
  date: '2024-03-13',
  title: 'Testing with @brainstack/inject: Mocking and Swapping Dependencies',
  description: 'Learn how to effectively test applications using @brainstack/inject, with practical examples of dependency mocking and swapping.',
  series: {
    name: '@brainstack/inject Series',
    order: 4
  },
  tags: ['dependency-injection', 'brainstack', 'inject', 'typescript', 'testing'],
  category: 'Testing'
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

// ...existing code...